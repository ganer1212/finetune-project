# mmpOS custom miner metadata for pearlfortune (Pearl/PRL NVIDIA miner)
EXTERNAL_NAME=pearlfortune
EXTERNAL_VERSION=1.1.8

# 可选:在此填入 token,mmp-launch.sh 会自动以 --token 传给矿工
# (也可以在 mmpOS profile 的额外参数里直接写 "--token <你的TOKEN>")
# TOKEN=
